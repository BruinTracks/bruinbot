services:
  - type: web
    name: bruinbot-server
    runtime: node
    rootDir: bruinbot_server
    buildCommand: npm run render-build
    startCommand: npm start
    envVars:
      - key: PYTHON_BIN
        value: python3
